hrs = input("Enter Hours:")
rate = input("Enter the Rate: ")
try:
    h = float(hrs)
    r = float(rate)
except:
    print("Enter a numerical value")
    quit()
if h > 40:
    pay = (h - 40) * (1.5 * r) + r * 40
else:
    pay = h * r
print('Pay:', pay)
